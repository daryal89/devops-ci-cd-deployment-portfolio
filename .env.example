# Application runtime environment
NODE_ENV=development

# HTTP port used by the application
PORT=3000

# Application version displayed by the /version endpoint
APP_VERSION=0.1.0

# Build or release identifier
BUILD_ID=local
